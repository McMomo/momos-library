{
  "name": "momos-library",
  "version": "0.1.0",
  "description": "A library of momos homebrews to dm some adventures",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy"
  },
  "keywords": [
    "dnd"
  ],
  "author": "Momo",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^1.0.1"
  }
}
